# Delivery Dashboard

A Streamlit dashboard for the delivery routing project.

## Run

```bash
streamlit run dashboard.py
```

## Data files

- `delivery_data.xlsx` → warehouse + delivery orders
- `fleet_data.xlsx` → vehicles + drivers
- `geocache.xlsx` → cached coordinates
